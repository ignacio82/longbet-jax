% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ordinal.R
\name{att_expected_score}
\alias{att_expected_score}
\title{Average treatment effect on an ordinal score}
\usage{
att_expected_score(object, weights = NULL, alpha = 0.05)
}
\arguments{
\item{object}{An ordinal `longbet.pred`, including a selected multi child.}

\item{weights}{Finite numeric vector of length K; defaults to `0:(K-1)`.
Weights need not be increasing.}

\item{alpha}{Tail probability for credible intervals.}
}
\value{
A list with `att` `[S]`, `intervals` `[2 x S]`, `att_full` `[S x draws]`,
  `exposure` and the chosen `weights`. Works in summary mode.
}
\description{
Forms weighted category effects within each draw before summarizing. Default
rank scores are a reporting convention, not an assumption that ordinal levels
have equal spacing. Indicator scores can request an exceedance probability.
}
